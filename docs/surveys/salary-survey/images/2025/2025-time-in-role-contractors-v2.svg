<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 660 355"><defs><style type="text/css">@import url(/_static/survey/css/2025-svg.css);#2025-time-in-role-contractors-v2 .bar-value-label,#2025-time-in-role-contractors-v2 .bar-value-label *{font-size:10px;font-weight:600;pointer-events:none}#2025-time-in-role-contractors-v2 .bar-value-label{transition:opacity 220ms ease-in-out,font-size 220ms ease-in-out,fill-opacity 220ms ease-in-out}#2025-time-in-role-contractors-v2 .bar-value-label.hover-only{opacity:0}#2025-time-in-role-contractors-v2 .bar-value-label.outside,#2025-time-in-role-contractors-v2 .bar-value-label.outside *{fill:var(--color-text-dark);font-size:10px;font-weight:600}</style><filter id="label-bg" width="130%" height="180%" x="-15%" y="-40%"><feMorphology in="SourceAlpha" operator="dilate" radius="3" result="expanded"/><feGaussianBlur in="expanded" result="blurred" stdDeviation="5"/><feComponentTransfer in="blurred" result="rounded"><feFuncA intercept="-5" slope="20" type="linear"/></feComponentTransfer><feFlood flood-color="#fff" flood-opacity=".8" result="color"/><feComposite in="color" in2="rounded" operator="in" result="bg"/><feMerge><feMergeNode in="bg"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs><g id="2025-time-in-role-contractors-v2" data-color-count="1"><g class="chart-header"><g class="chart-title"><text x="20" y="35" class="title-text">Length of time contracting</text><text x="20" y="55" class="subtitle-text">Contractor respondents (80)</text></g><g class="legend scale-single-series"><g class="legend-row"><g class="legend-item"><rect width="83" x="557" y="20" class="legend-item-container"/><circle cx="570" cy="31" class="series-a chart-color-1"/><text x="578" y="35" class="legend-label">Contractors</text></g></g></g></g><g class="chart-area bar-chart-vertical scale-single-series"><g class="y-axis"><g class="gridlines"><path d="M20 314.9H640"/><path d="M20 240.7H640"/><path d="M20 166.3H640"/><path d="M20 91.1H640"/></g><g class="axis-labels"><text font-size="10"><tspan x="25" y="317.9">0%</tspan></text><text font-size="10"><tspan x="20" y="243.7">10%</tspan></text><text font-size="10"><tspan x="20" y="169.3">20%</tspan></text><text font-size="10"><tspan x="20" y="94.1">30%</tspan></text></g></g><g class="data-series"><g class="a"><g class="axis-labels"><text data-bar-key="time-0-1" font-size="10"><tspan x="89.9" y="332.8">0-1 year</tspan></text><text data-bar-key="time-1-2" font-size="10"><tspan x="202.9" y="332.8">1-2 years</tspan></text><text data-bar-key="time-2-5" font-size="10"><tspan x="315.9" y="332.8">2-5 years</tspan></text><text data-bar-key="time-5-10" font-size="10"><tspan x="428.9" y="332.8">5-10 years</tspan></text><text data-bar-key="time-10-plus" font-size="10"><tspan x="544.9" y="332.8">10+ years</tspan></text></g><g class="bars chart-color-1 series-a"><rect width="105" height="122.4" x="62" y="193.1" data-bar-key="time-0-1" rx="2"/><rect width="105" height="150.5" x="175" y="165" data-bar-key="time-1-2" rx="2"/><rect width="105" height="178.6" x="288" y="136.9" data-bar-key="time-2-5" rx="2"/><rect width="105" height="150.5" x="401" y="165" data-bar-key="time-5-10" rx="2"/><rect width="105" height="150.5" x="514" y="165" data-bar-key="time-10-plus" rx="2"/></g><g class="value-labels"><text class="value-labels-inner bar-value-label inside" data-bar-key="time-0-1" filter="url(#label-bg)" text-anchor="middle"><tspan x="115" y="218">16.2%</tspan></text><text class="value-labels-inner bar-value-label inside" data-bar-key="time-1-2" filter="url(#label-bg)" text-anchor="middle"><tspan x="228" y="190">20.0%</tspan></text><text class="value-labels-inner bar-value-label inside" data-bar-key="time-2-5" filter="url(#label-bg)" text-anchor="middle"><tspan x="341" y="162">23.8%</tspan></text><text class="value-labels-inner bar-value-label inside" data-bar-key="time-5-10" filter="url(#label-bg)" text-anchor="middle"><tspan x="454" y="190">20.0%</tspan></text><text class="value-labels-inner bar-value-label inside" data-bar-key="time-10-plus" filter="url(#label-bg)" text-anchor="middle"><tspan x="567" y="190">20.0%</tspan></text></g></g></g></g></g><script type="application/ecmascript"><![CDATA[
    (function () {
      var root = document.querySelector('svg > g[id], g[id]');
      if (!root) {
        return;
      }

      var legendItem = root.querySelector('.legend-item');
      var legendContainer = legendItem ? legendItem.querySelector('rect.legend-item-container') : null;
      var legendLabel = legendItem ? legendItem.querySelector('text.legend-label') : null;
      var bars = Array.prototype.slice.call(root.querySelectorAll('.bars rect[data-bar-key]'));
      var axisLabels = Array.prototype.slice.call(root.querySelectorAll('.data-series > .a > .axis-labels text[data-bar-key]'));
      var valueLabels = Array.prototype.slice.call(root.querySelectorAll('.bar-value-label[data-bar-key]'));

      if (!legendItem || bars.length === 0 || axisLabels.length === 0 || valueLabels.length === 0) {
        return;
      }

      if (!legendItem.hasAttribute('tabindex')) {
        legendItem.setAttribute('tabindex', '0');
      }
      legendItem.style.cursor = 'pointer';

      if (legendContainer) {
        legendContainer.style.transition = 'opacity 220ms ease-in-out, stroke 220ms ease-in-out, stroke-width 220ms ease-in-out';
      }

      if (legendLabel) {
        legendLabel.style.transition = 'opacity 220ms ease-in-out';
      }

      function setLabelTypography(label, size, weight) {
        label.style.fontSize = size;
        label.style.fontWeight = weight;
        Array.prototype.forEach.call(label.querySelectorAll('tspan'), function (span) {
          span.style.fontSize = size;
          span.style.fontWeight = weight;
        });
      }

      bars.forEach(function (bar) {
        bar.style.cursor = 'pointer';
        bar.style.transition = 'fill-opacity 220ms ease-in-out, opacity 220ms ease-in-out';
      });

      axisLabels.forEach(function (label) {
        label.style.cursor = 'pointer';
        label.style.transition = 'opacity 220ms ease-in-out';
      });

      valueLabels.forEach(function (label) {
        if (!label.style.transition) {
          label.style.transition = 'opacity 220ms ease-in-out, font-size 220ms ease-in-out, fill-opacity 220ms ease-in-out';
        }
      });

      var activeMode = null;
      var activeBarKey = null;

      function renderState() {
        var isSeriesActive = activeMode === 'series';

        if (legendContainer) {
          legendContainer.style.stroke = isSeriesActive ? 'var(--color-text-dark)' : '';
          legendContainer.style.strokeWidth = isSeriesActive ? '1.5' : '';
        }

        bars.forEach(function (bar) {
          var isBarActive = activeMode === 'bar' && bar.getAttribute('data-bar-key') === activeBarKey;
          if (isSeriesActive || isBarActive) {
            bar.style.fillOpacity = '1';
          } else {
            bar.style.removeProperty('fill-opacity');
          }
        });

        valueLabels.forEach(function (label) {
          var isHoverOnly = label.classList.contains('hover-only');
          var isLabelActive = isSeriesActive || (activeMode === 'bar' && label.getAttribute('data-bar-key') === activeBarKey);

          if (isLabelActive) {
            label.style.opacity = '1';
            setLabelTypography(label, '14px', '700');
            label.style.fillOpacity = '1';
          } else {
            label.style.opacity = isHoverOnly ? '0' : '';
            setLabelTypography(label, '10px', '600');
            label.style.removeProperty('fill-opacity');
          }
        });
      }

      function activateSeries() {
        activeMode = 'series';
        activeBarKey = null;
        renderState();
      }

      function clearSeries() {
        if (activeMode === 'series') {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      function activateBar(barKey) {
        activeMode = 'bar';
        activeBarKey = barKey;
        renderState();
      }

      function clearBar(barKey) {
        if (activeMode === 'bar' && activeBarKey === barKey) {
          activeMode = null;
          activeBarKey = null;
          renderState();
        }
      }

      legendItem.addEventListener('mouseenter', activateSeries);
      legendItem.addEventListener('mouseleave', clearSeries);
      legendItem.addEventListener('focus', activateSeries);
      legendItem.addEventListener('blur', clearSeries);

      bars.forEach(function (bar) {
        var barKey = bar.getAttribute('data-bar-key');

        bar.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        bar.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      axisLabels.forEach(function (label) {
        var barKey = label.getAttribute('data-bar-key');

        label.addEventListener('mouseenter', function () {
          activateBar(barKey);
        });

        label.addEventListener('mouseleave', function () {
          clearBar(barKey);
        });
      });

      renderState();
    }());
  ]]></script></svg>